# Contributing

Use Node.js 22.12+, npm ci, npm run build, npm run typecheck, npm test and npm run audit.
Source and tests are TypeScript; all tests use Vitest and live under tests/.
Read AGENTS.md and ARCHITECTURE.md before changing routing or native lifecycle.
Use dedicated private Slack test channels and explicitly created test agents.
